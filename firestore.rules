rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ---------------------------
    // GLOBAL HELPERS
    // ---------------------------
    function isUserSignedIn() {
      return request.auth != null;
    }

    function getUserDoc() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid));
    }

    function isRootAdmin() {
      let userDoc = getUserDoc();
      return isUserSignedIn()
        && userDoc.data != null
        && 'roles' in userDoc.data
        && userDoc.data.roles.hasAny(['RootAdmin']);
    }

    function isChurchMember(churchId) {
      let userDoc = getUserDoc();
      return isUserSignedIn()
        && userDoc.data != null
        && 'churchId' in userDoc.data
        && userDoc.data.churchId == churchId;
    }

    function hasChurchRole(churchId, allowedRoles) {
      let userDoc = getUserDoc();
      return isChurchMember(churchId)
        && userDoc.data != null
        && 'roles' in userDoc.data
        && userDoc.data.roles.hasAny(allowedRoles);
    }

    // Group Manager roles
    function isGroupManager(churchId) {
      return hasChurchRole(churchId, [
        "MusicManager",
        "UsherManager",
        "MensGroupManager",
        "WomensGroupManager",
        "YouthManager"
      ]);
    }

    // Group Member roles
    function isGroupMember(churchId) {
      return hasChurchRole(churchId, [
        "Music",
        "Usher",
        "MensGroup",
        "WomensGroup",
        "Youth"
      ]);
    }

    // ---------------------------
    // NORMALIZE MANAGER ROLES → GROUP NAMES
    // ---------------------------
    function managerGroupsForUser() {
      let roles = getUserDoc().data.roles;

      // Build a new list manually
      let result = [];

      // Firestore Rules do not support forEach, so we use reduce
      return roles.reduce(result, role,
        role.endsWith("Manager")
          ? result.concat([role.replace("Manager", "").toLowerCase()])
          : result
      );
    }

    // ---------------------------
    // USERS
    // ---------------------------
    match /users/{uid} {

      allow read: if request.auth != null && request.auth.uid == uid;

      allow update: if request.auth != null
        && request.auth.uid == uid
        && request.resource.data.diff(resource.data)
           .changedKeys().hasOnly(["profilePhotoUrl"]);

      allow read, write: if isRootAdmin();

      allow read: if resource.data != null
        && hasChurchRole(resource.data.churchId, ["Admin"]);

      allow write: if request.resource.data != null
        && hasChurchRole(getUserDoc().data.churchId, ["Admin"])
        && request.resource.data.churchId == getUserDoc().data.churchId;
    }

    // ---------------------------
    // USER SETTINGS SUBCOLLECTION
    // ---------------------------
    match /users/{uid}/settings/{settingId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }

    // ---------------------------
    // CHURCHES ROOT
    // ---------------------------
    match /churches {
      allow list: if isRootAdmin();
    }

    // ---------------------------
    // CHURCH DOCUMENT
    // ---------------------------
    match /churches/{churchId} {
      allow get: if true;
      allow list: if false;

      // ---------------------------
      // CONTRIBUTIONS
      // ---------------------------
      match /contributions/{contributionId} {
        allow read: if isChurchMember(churchId);
        allow create, update, delete: if
          hasChurchRole(churchId, ["Admin", "Finance"]) ||
          isRootAdmin();
      }

      // ---------------------------
      // EVENTS SUBCOLLECTION
      // ---------------------------
      match /events/{eventId} {

        // Anyone in the church can read events
        allow read: if isChurchMember(churchId);

        // CREATE RULES
          // TEMP: strip this down to pure role check
          allow create: if
            request.resource.data != null &&
            (
              hasChurchRole(churchId, ["Admin"]) ||
              hasChurchRole(churchId, ["MusicManager"]) ||
              isRootAdmin()
            );

        // UPDATE RULES
        allow update: if
          resource.data != null &&
          (
            // Admins can update anything
            hasChurchRole(churchId, ["Admin"]) ||
            isRootAdmin() ||

            // Group managers can update their own private events only
            (
              isGroupManager(churchId) &&
              resource.data.isPublic == false &&
              resource.data.groups.hasAny(managerGroupsForUser()) &&
              request.resource.data.groups == resource.data.groups &&
              request.resource.data.isPublic == resource.data.isPublic
            )
          );

        // DELETE RULES
        allow delete: if
          hasChurchRole(churchId, ["Admin"]) ||
          isRootAdmin();

        // Root Admin override
        allow read, write: if isRootAdmin();
      }

      // ---------------------------
      // MEMBERS SUBCOLLECTION
      // ---------------------------
      match /members/{memberId} {

        allow read: if false;
        allow list: if false;

        allow read, write: if isRootAdmin();

        allow read, write: if hasChurchRole(churchId, ["Admin"]);

        allow read: if isChurchMember(churchId)
          && request.auth.uid == memberId;

        allow list: if request.query.where.size() == 2
          && request.query.where[0].field == "checkInCode"
          && request.query.where[1].field == "phoneNumber";

        allow write: if false;
      }

      // ---------------------------
      // ALL OTHER SUBCOLLECTIONS
      // ---------------------------
      match /{sub=**} {
        allow read, write: if isRootAdmin();
        allow read: if isChurchMember(churchId);
        allow write: if hasChurchRole(churchId, ["Admin"]);
      }
    }
  }
}
